@model IEnumerable<ShoppingMiddleware.Models.MauSac>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_MainLayout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenMau)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenMau)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDMau }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDMau }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDMau })
        </td>
    </tr>
}

</table>
